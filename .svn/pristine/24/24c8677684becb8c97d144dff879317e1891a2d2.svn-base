<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>数组查重</title>
</head>
<body>
<script>
   // var str="abc"
    //indexOf() 找出元素第一次出现的位置，元素未出现结果为-1
    var arr=["a","b","c","b","e"];
    var arr1=[];
    //alert(arr.indexOf("b"));
    /*for(var i=0;i<arr.length;i++){
        //遍历查询新数组中是否存在对应的元素
        if(arr1.indexOf(arr[i])==-1){
           // 没有数据，添加入新数组
            arr1.push(arr[i]);
            //第一次遍历结果得到的arr1["a"];
            //第一次遍历结果得到的arr1["a","b"];
            //第三次是[a,b,c]
            //第四次是[a,b,c]
            //第五次是[a,b,c,e]
        }
      alert(arr1);
    }*/
    for(var i=0;i<arr.length;i++)
    {
        arr1[arr[i]]="a";
    }
    //遍历arr的下标,下标唯一，重复的不读
    for(var index in arr1){
        document.write(index);
    }
    //tostring(),将元素转换为字符串
   var str=arr.toString();

</script>
</body>
</html>